

let scott = "Scott"; //what datatype is this
let age = 40; //what datatype is this
let isCool = true; //what data type is this
const object = {
    class: 'SEI 111',
    company: 'GA'
} //what data type is this 
const array = [1, 'hello', {name: 'John'}, true]; //what data type is this? (array)

function add(num1, num2){
    return num1 + num2
}//what is this? (function)

function multiply(num1, num2){
    return num1 * num2;
}

module.exports = {
    scott,
    age, 
    isCool,
    object,
    add, 
    array,
    multiply
}